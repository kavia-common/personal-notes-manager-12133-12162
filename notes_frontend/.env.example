# PUBLIC INTERFACES / CONFIG
# Base URL for API calls (can be empty to use local Next.js route handlers)
NEXT_PUBLIC_API_BASE_URL=
# If using external auth provider, specify the login redirect/callback URLs etc.
# For the built-in demo auth (localStorage), no config is required.
